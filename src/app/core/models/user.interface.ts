export interface IUser {
  id: number;
  name: string;
  friends: number;
  invitation: number;
  achievements: number;
  currentLeague: string;
  taskCompleted: number;
  userImageProfile: string;
  burningDays: number;
  tokenBalance: number;
}

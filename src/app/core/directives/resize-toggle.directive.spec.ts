import { ResizeToggleDirective } from './resize-toggle.directive';

describe('ResizeToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeToggleDirective();
    expect(directive).toBeTruthy();
  });
});
